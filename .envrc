# direnv configuration — run `direnv allow` after cloning

# Add local binaries to PATH (enables `nest`, `jest`, etc. without npx)
PATH_add node_modules/.bin

export NODE_ENV=development
export PORT=3000

# Load .env for secrets and overrides (never committed)
dotenv_if_exists .env
